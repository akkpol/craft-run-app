Use the approved workflow model from the previous step and design the database changes needed for implementation.

Your task:
1. Inspect the current schema, migrations, and TypeScript types in the workspace
2. Propose the minimal schema evolution needed to support the new workflow
3. Reuse existing tables where sensible
4. Introduce new tables only when clearly justified
5. Preserve compatibility with the current quote approval and job lifecycle where possible

Design for these concerns:
- workflow state progression
- payment gating
- design assignment and preview approval
- fulfillment mode: pickup vs delivery
- completion package / document status
- customer-input hold states
- human review escalation
- future AI vs human design execution using metadata, not primary states

Expected output:
1. Schema change summary
2. Table-by-table changes
3. New columns with types and purpose
4. New tables, if any, with justification
5. Migration plan order
6. Backfill strategy for existing rows
7. TypeScript type updates needed
8. Risks / tradeoffs

Then implement:
- create the migration SQL
- update the TypeScript constants and types
- keep names consistent and explicit
- avoid using any
- keep the solution minimal and maintainable

Important:
- Do not redesign the entire database if a smaller evolution works
- Do not break existing public token flows
- Use the current project conventions
- Explain any field that is metadata vs business state