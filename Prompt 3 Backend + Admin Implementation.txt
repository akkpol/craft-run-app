Implement the new workflow model and schema into the existing app.

Scope:
- update backend workflow logic
- update admin actions and dashboard behavior
- keep customer-facing flows working
- preserve current quote and status pages unless changes are required by the new workflow

Your task:
1. Inspect the current API routes, workflow helpers, admin pages, and status/quote pages
2. Update workflow transitions to the new state model
3. Enforce transition rules in backend code
4. Update admin actions so users can only perform valid next actions
5. Ensure payment-gated flows do not enter production early
6. Support human review and customer-input hold paths cleanly
7. Represent AI vs human design assignment as metadata fields, not state branching
8. Keep the implementation incremental and compatible with current behavior where possible
9. Add or update tests if the repo already has a testing pattern; if not, add light verification where appropriate

Files likely involved:
- webhook intake flow
- intake submission flow
- quote approval flow
- job/status update flow
- workflow helper utilities
- admin dashboard and action controls
- customer status display if needed

Required outcomes:
- invalid transitions are blocked server-side
- admin UI exposes valid actions only
- workflow state and operational metadata stay consistent
- existing happy path still works
- escalation / hold flows are explicit

Output:
1. Brief implementation plan
2. Code changes
3. Summary of new transition behavior
4. Any residual gaps not implemented

Important:
- Do not do a giant rewrite
- Prefer smaller iterative edits
- Fix root causes, not superficial mappings
- Keep the code readable for future expansion